{
  "mcpServers": {
    "benefit-navi": {
      "type": "stdio",
      "command": "python3",
      "args": ["${CLAUDE_PROJECT_DIR:-.}/mcp_server.py"],
      "env": {
        "BENEFIT_NAVI_API_URL": "${BENEFIT_NAVI_API_URL:-https://dev.benefit-navi.com}",
        "BENEFIT_NAVI_API_TOKEN": "${BENEFIT_NAVI_API_TOKEN}"
      }
    }
  }
}
