# benefit-navi

Docker Compose (Flask + PostgreSQL)

The web container listens on port 8000 internally and publishes to host port 8080 by default.
Set `WEB_PORT` to override the host port when needed.
